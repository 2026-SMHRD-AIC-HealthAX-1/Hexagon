# 테이블 명세서

**과제명 :** Eye God — 안구 건조증 기반 백내장 위험 조기 예측 서비스
**작성일 :** 2026. 09. 08.

> 참고: 실제 구현은 SQLite(stdlib `sqlite3`)로 되어 있으나, 본 문서는 "정식 DB(PostgreSQL 등)"
> 전환을 염두에 두고 정식 컬럼 타입/제약조건 기준으로 작성했습니다. CREATE TABLE 스크립트는
> PostgreSQL 문법을 기준으로 했습니다.

## Ⅰ. 테이블 목록

| 테이블명 | 테이블_ID | 비고 |
|---|---|---|
| 회원 | USERS | Google 실제 OAuth + 카카오 실제 OAuth 로그인 공용 |
| 게임기록 | GAME_RECORDS | 시선추적게임(S-05)·리듬게임 점수 이력 |
| 분석결과 | ANALYSIS_RESULTS | S-03 사진분석용 스키마, 현재 미구현(값 항상 NULL) |
| 캘리브레이션데이터 | CALIBRATION_DATA | 사용자당 1행, 웹 경로 전용(CLI는 파일 사용) |

## Ⅱ. 테이블 정의서

### 1) USERS

|  | 작성일자 | 2026-09-08 |  | 작성자 |  |
|---|---|---|---|---|---|
| 테이블명 | 회원 |  | 테이블 ID | USERS |  |
| 테이블설명 | Google 실제 OAuth 로그인 및 카카오 실제 OAuth 로그인 사용자 정보를 저장하는 테이블 | | | | |

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| 회원번호 | ID | BIGINT | - | Y | Y | N | N | N | AUTO INCREMENT |
| 로그인제공자 | PROVIDER | VARCHAR | 20 | Y | N | N | N | Y | 'google' 또는 'kakao'만 허용 |
| 가입일시 | CREATED_AT | TIMESTAMP | - | Y | N | N | N | N | |
| 최근로그인일시 | LAST_LOGIN_AT | TIMESTAMP | - | Y | N | N | N | N | |
| 구글고유식별자 | GOOGLE_SUB | VARCHAR | 255 | N | N | N | Y | N | Google 로그인 사용자만 값 존재 |
| 카카오고유식별자 | KAKAO_ID | VARCHAR | 255 | N | N | N | Y | N | 카카오 로그인 사용자만 값 존재 |
| 이메일 | EMAIL | VARCHAR | 255 | N | N | N | N | N | Google은 항상, 카카오는 이메일 동의항목 활성화 시에만 값 존재 |

**Table 생성 스크립트**
```sql
CREATE TABLE USERS (
    ID              BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    PROVIDER        VARCHAR(20) NOT NULL CHECK (PROVIDER IN ('google', 'kakao')),
    CREATED_AT      TIMESTAMP NOT NULL,
    LAST_LOGIN_AT   TIMESTAMP NOT NULL,
    GOOGLE_SUB      VARCHAR(255) UNIQUE,
    KAKAO_ID        VARCHAR(255) UNIQUE,
    EMAIL           VARCHAR(255)
);
```

---

### 2) GAME_RECORDS

|  | 작성일자 | 2026-09-08 |  | 작성자 |  |
|---|---|---|---|---|---|
| 테이블명 | 게임기록 |  | 테이블 ID | GAME_RECORDS |  |
| 테이블설명 | 시선추적게임(S-05)과 리듬게임의 플레이 점수를 게임 종류별로 구분하여 저장하는 테이블 | | | | |

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| 기록번호 | ID | BIGINT | - | Y | Y | N | N | N | AUTO INCREMENT |
| 회원번호 | USER_ID | BIGINT | - | Y | N | Y | N | N | USERS.ID 참조 |
| 게임종류 | GAME_TYPE | VARCHAR | 20 | Y | N | N | N | Y | 'gaze' 또는 'rhythm', 기본값 'gaze' |
| 점수 | SCORE | INTEGER | - | Y | N | N | N | N | |
| 플레이일시 | PLAYED_AT | TIMESTAMP | - | Y | N | N | N | N | |

**Table 생성 스크립트**
```sql
CREATE TABLE GAME_RECORDS (
    ID          BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    USER_ID     BIGINT NOT NULL REFERENCES USERS(ID),
    GAME_TYPE   VARCHAR(20) NOT NULL DEFAULT 'gaze' CHECK (GAME_TYPE IN ('gaze', 'rhythm')),
    SCORE       INTEGER NOT NULL,
    PLAYED_AT   TIMESTAMP NOT NULL
);
```

---

### 3) ANALYSIS_RESULTS

|  | 작성일자 | 2026-09-08 |  | 작성자 |  |
|---|---|---|---|---|---|
| 테이블명 | 분석결과 |  | 테이블 ID | ANALYSIS_RESULTS |  |
| 테이블설명 | 사진 기반 백내장 위험도·충혈도 분석 결과 저장용 테이블 (S-03, 현재 스키마만 존재하고 실제 분석 로직은 미구현) | | | | |

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| 분석번호 | ID | BIGINT | - | Y | Y | N | N | N | AUTO INCREMENT |
| 회원번호 | USER_ID | BIGINT | - | Y | N | Y | N | N | USERS.ID 참조 |
| 백내장위험도 | CATARACT_RISK | VARCHAR | 20 | N | N | N | N | N | 현재 미구현, 항상 NULL |
| 충혈도 | REDNESS | VARCHAR | 20 | N | N | N | N | N | 현재 미구현, 항상 NULL |
| 분석일시 | ANALYZED_AT | TIMESTAMP | - | Y | N | N | N | N | |

**Table 생성 스크립트**
```sql
CREATE TABLE ANALYSIS_RESULTS (
    ID              BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    USER_ID         BIGINT NOT NULL REFERENCES USERS(ID),
    CATARACT_RISK   VARCHAR(20),
    REDNESS         VARCHAR(20),
    ANALYZED_AT     TIMESTAMP NOT NULL
);
```

---

### 4) CALIBRATION_DATA

|  | 작성일자 | 2026-09-08 |  | 작성자 |  |
|---|---|---|---|---|---|
| 테이블명 | 캘리브레이션데이터 |  | 테이블 ID | CALIBRATION_DATA |  |
| 테이블설명 | 사용자별 시선 추적 캘리브레이션 결과 저장 테이블 (웹 경로 전용, 사용자당 1행 · 재캘리브레이션 시 덮어쓰기) | | | | |

| 컬럼명 | 컬럼 ID | 타입 | 길이 | NN | PK | FK | UK | CK | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| 회원번호 | USER_ID | BIGINT | - | Y | Y | Y | N | N | USERS.ID 참조, 1인당 1행 |
| 캘리브레이션데이터 | DATA | TEXT | - | Y | N | N | N | N | JSON 문자열 ({version, screen, samples}) |
| 갱신일시 | UPDATED_AT | TIMESTAMP | - | Y | N | N | N | N | |

**Table 생성 스크립트**
```sql
CREATE TABLE CALIBRATION_DATA (
    USER_ID     BIGINT PRIMARY KEY REFERENCES USERS(ID),
    DATA        TEXT NOT NULL,
    UPDATED_AT  TIMESTAMP NOT NULL
);
```
